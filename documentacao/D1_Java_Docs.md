<h2 align="center">CadMototaxista - Documentação Técnica</h2>

### D.1 – Documentação JavaDocs

A documentação JavaDocs do sistema **CADMOTOTAXISTA** foi integralmente gerada a partir dos comentários estruturados presentes no código-fonte, seguindo o padrão oficial da linguagem Java. Essa documentação descreve de forma detalhada as classes, interfaces, métodos e atributos que compõem o sistema, fornecendo uma visão completa da arquitetura e do comportamento de cada componente.

O uso de JavaDocs é fundamental para garantir `clareza`, `rastreabilidade` e `manutenção do código`. Por meio dela, desenvolvedores e colaboradores podem compreender rapidamente as responsabilidades de cada módulo, a estrutura das entidades de domínio, a lógica de negócio aplicada e a interação entre as camadas da aplicação (Controller, Service, Repository e Model).

Além disso, a documentação JavaDocs contribui diretamente para a `padronização do desenvolvimento`, reduzindo a curva de aprendizado de novos integrantes da equipe e fortalecendo a qualidade técnica e a transparência do projeto junto à Secretaria Municipal de Trânsito e Transporte (SMTT).

A documentação completa pode ser acessada no diretório `/documentacao/site/apidocs`.
Para visualizá-la localmente, é necessário baixar a pasta `apidocs` do repositório e abrir o arquivo `index.html`, que serve como ponto de entrada para toda a documentação gerada automaticamente pelo `JavaDocs`.

Alternativamente, o conteúdo também pode ser consultado on-line, por meio da documentação hospedada no Netlify, disponível no endereço abaixo:

#### 🔗 Acessar Documentação JavaDocs – [Netlify](https://documentacao-cadmototaxista-javadocs.netlify.app/)